<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />
    <style name="CalculatorButton">
        <item name="android:layout_width">80dp</item>
        <item name="android:layout_height">80dp</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:textSize">32sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:borderlessButtonStyle">?android:borderlessButtonStyle</item>
    </style>

    <style name="CalculatorButton.DarkGray" parent="CalculatorButton">
        <item name="android:background">@drawable/button_background_dark_gray</item>
    </style>

    <style name="CalculatorButton.DarkGray.Zero" parent="CalculatorButton.DarkGray">
        <item name="android:layout_width">176dp</item>
        <item name="android:gravity">start|center_vertical</item>
        <item name="android:paddingStart">30dp</item>
        <item name="android:paddingEnd">0dp</item>
    </style>

    <style name="CalculatorButton.LightGray" parent="CalculatorButton">
        <item name="android:background">@drawable/button_background_light_gray</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="CalculatorButton.Orange" parent="CalculatorButton">
        <item name="android:background">@drawable/button_background_orange</item>
    </style>
</resources>